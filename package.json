{
  "name": "my-portfolio",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter portfolio dev",
    "build": "pnpm --filter portfolio build",
    "ds:dev": "pnpm --filter design-system dev",
    "lint": "pnpm -r lint",
    "sync": "tsx sync-content.ts"
  },
  "devDependencies": {
    "@notionhq/client": "^2.3.0",
    "tsx": "^4.19.4",
    "typescript": "^5.4.5"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  }
}
